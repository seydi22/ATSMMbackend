# Variables à configurer dans Vercel → Project → Settings → Environment Variables

MONGODB_URI=mongodb+srv://...
JWT_SECRET=secret-fort
OPERATOR_USERNAME=admin
OPERATOR_PASSWORD=mot-de-passe-fort

SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=ATS Portal <s.dieng@moov-money.mr>

# URL du frontend Vercel (CORS), ex: https://ats-portal.vercel.app
FRONTEND_ORIGIN=*
